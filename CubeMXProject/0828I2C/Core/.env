# 複製這個檔案為 .env（去掉 .example），並填入你自己的值。
# .env 本身不要加入版本控制（記得加進 .gitignore）。

# PostgreSQL 連線字串
DB_DSN=postgresql://postgres:current@localhost:5432/rtc3231

# 序列埠。留空或整行刪掉的話，程式會自動呼叫 auto_detect() 搜尋。
# RTC_PORT=COM3
